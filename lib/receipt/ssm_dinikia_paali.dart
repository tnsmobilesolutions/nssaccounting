import 'package:flutter/material.dart';

class SSMDinikiaPaali extends StatelessWidget {
  const SSMDinikiaPaali({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SSM Dinikia Paali'),
      ),
      body: Container(
        child: Text("SSM Dinikia Paali"),
      ),
    );
  }
}
