import 'package:flutter/material.dart';

class Samilani extends StatelessWidget {
  const Samilani({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hi'),
    );
  }
}
