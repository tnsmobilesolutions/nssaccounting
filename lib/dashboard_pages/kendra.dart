import 'package:flutter/material.dart';

class Kendra extends StatelessWidget {
  const Kendra({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hi'),
    );
  }
}
