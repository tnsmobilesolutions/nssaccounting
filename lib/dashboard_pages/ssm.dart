import 'package:flutter/material.dart';

class SSM extends StatelessWidget {
  const SSM({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hi'),
    );
  }
}
